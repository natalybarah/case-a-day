'use server';

import {sql} from '../lib/neondb';

//import '../../public/assets/waitlikeitshard.png'
/*
export async function createCollection(){
  const newCollection=  await sql`INSERT INTO collections (title, summary, slug, image, chips) VALUES (${"wait like it's hard"}, 'Explore the landmark rulings driven by female advocates who shattered legal ceilings. Whether standing before the bench or architecting the groundbreaking strategy behind the scenes, these women dismantled systemic discrimination and permanently altered the course of American history.',
    'wait-like-its-hard', '../../public/assets/waitlikeitshard.png' ,${['new']})`;
    return newCollection
}

*/